package com.flat.nsd;

/**
 * The types and formats of messages sent between devices over NSD.
 *
 * @author Jacob Phillips (01/2015, jphilli85 at gmail)
 */
public class MessageProtocol {
    private static final String TAG = MessageProtocol.class.getSimpleName();

    //public static final String TYPE_MAC = "mac";
    public static final String TYPE_MAC = "mac";

    // TODO reference frame (range tables) is only needed type?
}
